<a
	class="kengine-link-root"
	role="{{effectiveAccRole}}"
	href="{{parsedRef}}"
	target="{{target}}"
	rel="{{_rel}}"
	tabindex="{{effectiveTabIndex}}"
	title="{{title}}"
	?disabled="{{disabled}}"
	aria-label="{{ariaLabelText}}"
	aria-haspopup="{{accessibilityAttributes.hasPopup}}"
	aria-expanded="{{accessibilityAttributes.expanded}}"
	@focusin={{_onfocusin}}
	@focusout={{_onfocusout}}
	@click={{_onclick}}
	@keydown={{_onkeydown}}
	@keyup={{_onkeyup}}>
	<slot></slot>
	{{#if hasLinkType}}
		<span class="kengine-hidden-text">{{linkTypeText}}</span>
	{{/if}}
</a>
