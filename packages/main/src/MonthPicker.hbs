<div
	class="kengine-mp-root"
	role="grid"
	aria-roledescription="{{roleDescription}}"
	aria-readonly="false"
	aria-multiselectable="false"
	@keydown={{_onkeydown}}
	@keyup={{_onkeyup}}
	@click={{_selectMonth}}
>
	{{#each _months}}
		<div class="kengine-mp-quarter">

			{{#each this}}
				<div
					data-sap-timestamp={{this.timestamp}}
					tabindex={{this._tabIndex}}
					?data-sap-focus-ref="{{this.focusRef}}"
					class="{{this.classes}}"
					role="gridcell"
					aria-selected="{{this.ariaSelected}}"
				>
					<span class="kengine-dp-monthtext">
						{{this.name}}
					</span>
					{{#if this.nameInSecType}}
						<span class="kengine-dp-monthtext kengine-dp-monthsectext">
							{{this.nameInSecType}}
						</span>
					{{/if}}
				</div>
			{{/each}}

		</div>
	{{/each}}
</div>
