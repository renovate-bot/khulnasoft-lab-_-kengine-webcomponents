<div
	class="{{classes.root}}"
	role="region"
	aria-label="{{_getAriaLabel}}">
	{{#if _hasHeader}}
		<div class="kengine-card-header-root">
			<slot name="header"></slot>
		</div>
	{{/if}}
	<div role="group" aria-label="{{_ariaCardContentLabel}}">
		<slot></slot>
	</div>
</div>
