<div class="kengine-radio-root"
	role="radio"
	aria-checked="{{checked}}"
	aria-disabled="{{effectiveAriaDisabled}}"
	aria-describedby="{{effectiveAriaDescribedBy}}"
	aria-label="{{ariaLabelText}}"
	tabindex="{{effectiveTabIndex}}"
	@click="{{_onclick}}"
	@keydown="{{_onkeydown}}"
	@keyup="{{_onkeyup}}"
	@mousedown="{{_onmousedown}}"
	@mouseup="{{_onmouseup}}"
	@focusout="{{_onfocusout}}"
>
	<div class='kengine-radio-inner {{classes.inner}}'>
		<svg class="kengine-radio-svg" focusable="false" aria-hidden="true">
			<circle class="kengine-radio-svg-outer" cx="50%" cy="50%" r="50%" />
			<circle class="kengine-radio-svg-inner" cx="50%" cy="50%" />
		</svg>
		<input type='radio' ?required="{{required}}" ?checked="{{checked}}" ?readonly="{{readonly}}" ?disabled="{{effectiveAriaDisabled}}" name="{{name}}"  data-sap-no-tab-ref/>
	</div>

	{{#if text}}
		<kengine-label id="{{_id}}-label" class="kengine-radio-label" for="{{_id}}" wrapping-type="{{wrappingType}}">{{text}}</kengine-label>
	{{/if}}

	{{#if hasValueState}}
		<span id="{{_id}}-descr" class="kengine-hidden-text">{{valueStateText}}</span>
	{{/if}}
	<slot name="formSupport"></slot>
</div>
