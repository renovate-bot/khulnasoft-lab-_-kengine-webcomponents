<div class="kengine-block-layer"
    ?hidden={{_blockLayerHidden}}
    tabindex="0"
    style="{{styles.blockLayer}}"
	@keydown="{{_preventBlockLayerFocus}}"
    @mousedown="{{_preventBlockLayerFocus}}">
</div>
